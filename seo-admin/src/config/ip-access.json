{
  "$schema": "Configuration file to restrict domain access to specific IP addresses only",
  "enabled": true,
  "rules": {
    "admin.vytron.me": {
      "description": "Restricted admin domain — accessible only by specified whitelist IPs",
      "allowedIps": [
        "223.233.78.49",
        "::1"
      ]
    },
    "seo.vytron.me": {
      "description": "Internal staging and preview domain",
      "allowedIps": [
        "223.233.78.49",
        "::1"
      ]
    }
  }
}